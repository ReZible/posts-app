import { Post } from '@/entities/post';

const initialPostsData: Post[] = [];

const initialCurrentPage: number = 1;

const initialTotalCount: number = 1;

const initialFetchingStatus: boolean = true;

const initialStartPosition: number = 0;

export {
	initialPostsData,
	initialCurrentPage,
	initialFetchingStatus,
	initialTotalCount,
	initialStartPosition,
};
